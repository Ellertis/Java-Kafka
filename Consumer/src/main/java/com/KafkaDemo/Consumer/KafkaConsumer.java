package com.KafkaDemo.Consumer;

/*import org.springframework.kafka.annotation.KafkaListener;
import org.springframework.stereotype.Component;

@Component
public class KafkaConsumer {
    @KafkaListener(topics = "test-topic", groupId = "test-group")
    public void listen1(String message){
        System.out.println("Received Message 1: " + message);
    }
    @KafkaListener(topics = "config-topic", groupId = "test-group")
    public void listenObject(Package mypackage){
        System.out.println("Received Object:" + "Package Name : " + mypackage.getPackageName() + " Package price : " + mypackage.getPrice() + " Package weight : " + mypackage.getWeight());
    }
}
*/
public class KafkaConsumer {}